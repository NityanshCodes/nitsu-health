# Naming Conventions

- Use lowercase snake_case for table and column names.
- Prefer singular table names for core entities.
- Use `id` as the primary key for each table.
- Use `created_at` and `updated_at` for audit timestamps.
