# Indexing Strategy

Planned indexes include:
- users(email)
- users(username)
- medical_records(user_id, created_at)
- reports(user_id, created_at)
- notifications(user_id, created_at)
- sessions(user_id, expires_at)
